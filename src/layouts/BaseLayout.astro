---
import SEO from '@components/SEO.astro';
import Nav from '@components/Nav.astro';
import '@styles/global.css';

interface Props {
  title: string;
  description: string;
  noindex?: boolean;
  canonical?: string | undefined;
  ogImage?: string;
  navVariant?: 'dark' | 'light';
}

const {
  title,
  description,
  noindex = false,
  canonical,
  ogImage,
  navVariant = 'dark',
} = Astro.props;

const user = Astro.locals.user
  ? { email: Astro.locals.user.email ?? '' }
  : null;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&family=Plus+Jakarta+Sans:ital,wght@0,400;0,500;0,600;1,400&family=JetBrains+Mono:wght@400;500&display=swap"
      rel="stylesheet"
    />
    <SEO
      title={title}
      description={description}
      noindex={noindex}
      canonical={canonical}
      ogImage={ogImage}
    />
  </head>
  <body>
    <Nav user={user} variant={navVariant} />
    <main>
      <slot />
    </main>
    <footer class="site-footer">
      <div class="container">
        <div class="site-footer__inner">
          <span class="site-footer__logo">wisdo<span class="dot">.</span></span>
          <p class="site-footer__copy">
            &copy; {new Date().getFullYear()} wisdo. El marketplace de herramientas para equipos de ventas.
          </p>
          <nav class="site-footer__links" aria-label="Footer">
            <a href="/tools/">Market</a>
            <a href="/blog/">Blog</a>
            <a href="/about/">Sobre wisdo</a>
          </nav>
        </div>
      </div>
    </footer>
  </body>
</html>

<style>
  .site-footer {
    background-color: var(--color-dark);
    color: rgb(255 255 255 / 0.5);
    padding: var(--space-10) 0;
    margin-top: auto;
  }

  .site-footer__inner {
    display: flex;
    align-items: center;
    gap: var(--space-6);
    flex-wrap: wrap;
  }

  .site-footer__logo {
    font-family: var(--font-display);
    font-size: var(--text-lg);
    font-weight: var(--font-bold);
    color: white;
    letter-spacing: -0.02em;
  }

  .site-footer__logo .dot {
    color: var(--color-green);
  }

  .site-footer__copy {
    font-size: var(--text-sm);
    margin-right: auto;
  }

  .site-footer__links {
    display: flex;
    gap: var(--space-6);
  }

  .site-footer__links a {
    font-size: var(--text-sm);
    color: inherit;
    transition: color var(--transition-fast);
  }

  .site-footer__links a:hover {
    color: white;
  }

  @media (max-width: 640px) {
    .site-footer__inner {
      flex-direction: column;
      align-items: flex-start;
    }

    .site-footer__copy {
      margin-right: 0;
    }
  }
</style>
